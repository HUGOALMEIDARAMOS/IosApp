import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.hugoalmeida',
  appName: 'CalcSuper',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
